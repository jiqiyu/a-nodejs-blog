<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
    <script>
      var pageStartTime = Date.now();
      function putsRenderingTime() {
        var pageReadyTime = Date.now();
        pageRenderingTime = pageReadyTime - pageStartTime;
        console.log('數據庫查詢次數：' + <%= dbtimes %> +
                    '，頁面加載時間' + pageRenderingTime + 'ms' +
                    '：DOM ' + domRenderingTime + 'ms' +
                    '，內容' + (pageRenderingTime - domRenderingTime) + 'ms');
      }
      window.onload = putsRenderingTime;
    </script>
    <script src="/javascripts/531ae90180772f947d000001.js"></script>
    <link rel='stylesheet' href='/stylesheets/reset.css'>
    <link rel='stylesheet' href='/stylesheets/style.css'>
    <link rel="shortcut icon" rel="icon" href="/images/m-99ffff.ico" type="image/x-icon">
    <script src="/javascripts/531ae90180772f947d000003.js"></script>
    <script>hljs.initHighlightingOnLoad();</script>
    <!-- <link href='http://fonts.googleapis.com/css?family=Plaster' rel='stylesheet' type='text/css'> -->
  </head>
  <body>
    <header class="site-head">
      <h1 class="logo">
        <a href="/" title="mingri.org" alt="mingri.org">
          www.<img src="/images/m-66ffff.png" style="margin: 0 0.1em;">.ORG
        </a>
      </h1>
      <nav class="user-state">
        <% if (name) { %>
        <span>用戶<%= name %>&nbsp;|&nbsp;</span>
        <% if (level && (level >= ul.author) ) { %>
        <span><a href="/control-panel">去控制台&nbsp;|&nbsp;</a></span>
        <% } %>
        <span><a href="/logout">退出</a></span>
        <% } else { %>
        <span><a href="/login">登錄</a></span>
        <% } %>
      </nav>
    </header>

